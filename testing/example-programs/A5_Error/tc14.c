int main()
{
 float f1,f2;
 int x1,x2;
 f1=0.45;
 f2=0.44;
 x1=10;
 x2=20;
 (f1>f2) || (x1>x2) ? "Larger number found" : "smaller number found";
 // Not allowed if left side is string variable to store the result then it's allow.
}
