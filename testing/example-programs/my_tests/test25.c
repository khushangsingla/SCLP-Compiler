string ss;
float c;
int aaaa;
bool sjh;
int fn(int x, string y, float z, int not)
{
	int main;
	print x;
	print y;
	print z;
	print not;
	print main;

	return 1;
}
void main(){
	int x;
	float y;
	string z;
	bool a;
	// x = 1;
	// a = y>0.2 || y<0.2 || y == 0.2 || y != 0.2 || y>=0.2 || y<=0.2;
	// x= -x + 1 * x / x;
	// y= -y + 1.0 * y / y;
	
	x = a ? 1 : 2;
	x = fn(1, "abc", 1.0, 1);
	x = fn(x, z, y, x);
	// if(a){
	// 	z = "abc";
	// }
	// else{
	// 	y = 3.0;
	// }
	// print x;
	// print y;
	// print z;
	// read x;
	print aaaa;
}
